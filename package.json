{
  "name": "blocked-fetch",
  "version": "1.0.0",
  "private": true,
  "description": "Fetch pages from sites that block curl/bots, via browser session + DuckDuckGo-hop unlock",
  "dependencies": {
    "playwright-core": "^1.50.0"
  }
}
