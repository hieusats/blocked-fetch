{
  "name": "blocked-fetch",
  "version": "1.1.0",
  "private": true,
  "description": "Fetch pages from sites that block curl/bots: curl-UA → browser → search-hop → stealth Chromium",
  "scripts": {
    "selftest": "bash scripts/selftest.sh"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "cloakbrowser": "^0.5.8",
    "jsdom": "^30.0.1",
    "playwright-core": "^1.50.0",
    "turndown": "^7.2.4",
    "unpdf": "^1.8.1"
  }
}
